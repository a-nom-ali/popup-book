These original SVG thumbnails are projected from the same paper geometry, embedded artwork and original digital meshes used by each interactive demo. They use an orthographic camera and a lightweight vector renderer; the live viewer adds full lighting and material effects.

Regenerate after changing a demo or digital model:

```sh
node src/assets/demos/generate.mjs
```
